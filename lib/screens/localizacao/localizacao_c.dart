import 'package:flutter/material.dart';

class LocalizacaoC extends StatelessWidget {
  const LocalizacaoC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
